import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  checkbox: {
    position: 'relative',
    width: 19,
    height: 19,
    borderRadius: 4,
    justifyContent: 'center',
    alignItems: 'center',
  },
  checkIcon: {
    position: 'absolute',
  },
});
